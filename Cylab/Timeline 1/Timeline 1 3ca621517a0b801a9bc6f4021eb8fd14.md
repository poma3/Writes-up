# Timeline 1

- kiểm tra thì thấy đây là một patition trên ext4
    
    ```jsx
    └─$ file partition4.img
    partition4.img: Linux rev 1.0 ext4 filesystem data, UUID=7a00e9da-98f8-4f0f-b257-95edf422d902 (extents) (64bit) (large files) (huge files)
    ```
    
- phân tích cấu trúc thư mục và trích xuất thời gian
    
    ```jsx
    fls -r -m / partition4.img > body.txt
    ```
    
- dùng mactime để sắp xếp theo thứ tự time tăng dần
    
    ```jsx
    mactime -b body.txt > timeline.txt
    ```
    
- đề bài gợi í là filter macb để lọc ra những file vừa mới được tạo. Khi một file được tạo lần đầu và chưa truy cập thì nó sẽ có dữ liệu thời gian là macb
- lọc và in ra 30 file được tạo gần đây nhất
    
    ![image.png](Timeline%201/image.png)
    
- thấy file /root/.ash_history lưu lịch sử lệnh của người dùng mà chỉ có 9 byte, và file này lại là file vừa được tạo

![image.png](Timeline%201/image%201.png)

- chỉ còn lịch sử của lệnh poweroff → attacker đã xóa toàn bộ file này và khi tắt máy nó tự tạo ra file này với lệnh poweroff
- ngay trên đó có /etc/chat là một file không tồn tại trong
    
    ![image.png](Timeline%201/image%202.png)
    
- decode base64 ra được `573417h13r_7h4n_7h3_1457_58527bb222`